import styled from 'styled-components';

export const HomeWrapper = styled.div`
  overflow-y: hidden; 
  text-align: center;
  display: grid;
  grid-template-rows: [timeline] 1fr [cta-row] 1fr;
  padding: 0;
  margin: 0;
`;
