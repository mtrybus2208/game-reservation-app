import React from 'react';
import PropTypes from 'prop-types'; 

import Ruler from './styles';


const propTypes = { };

const TimeRuler = ({timeScope}) => ( 
  
);

TimeRuler.propTypes = propTypes;
export default TimeRuler;
